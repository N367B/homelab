# Local AI assistant workspace/config files
.codex
.codex/
.claude/
.opencode/
.cursor/
.continue/
.aider*

# Secrets hygiene — only SOPS-encrypted files may carry credentials
.env
*.env
!*.sops.env
keys.txt
age.key
*.agekey

# Runtime state
scripts/ml350_fan_controller/ml350_fan.state.json
scripts/ml350_fan_controller/ml350_fan.state.tmp

# OpenTofu local state/caches (state should never hold plaintext in git anyway)
.terraform/
*.tfstate
*.tfstate.*
